import Element from "../model/Element";
//deci cand pun din server doar schimb ma gandesc linia asta cu sa vina in loc de din fisieru local sa vina de la server gien
import { elements } from "./periodic-table.json";
const elementList = elements as Element[];

export default elementList;
